#include <stdio.h>
#include "main.h"

/**
 * mul - Multiply two numbers (a and b).
 * @a: The first number to be multiplied.
 * @b: The number to be multiplied against.
 *
 * Return: The product of 'a' and 'b'.
 */
int mul(int a, int b)
{
	return (a * b);
}
